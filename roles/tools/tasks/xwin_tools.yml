---
# tasks file for tools
- name: Install xwin tools
  when: ansible_facts['distribution'] == 'Archlinux'
  become: yes
  pacman:
    name:
      - remmina
      - freerdp
      - flameshot
      - chromium
      - tdrop
    state: installed
